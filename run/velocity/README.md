## Setup
Setup to use the provided development scripts

- Add a velocity jar, and rename it to "velocity.jar".
- Start the server, it will generate the correct folder, stop it again
- Setup the fabric backend, and setup modern velocity forwarding
- Run the /run/velocity.sh script to generate the plugin if this plugin
- Use the main README.md to configure the config